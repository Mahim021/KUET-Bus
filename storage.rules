rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // TEST MODE: allow all reads and writes while debugging.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
