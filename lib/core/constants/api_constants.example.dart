/// EXAMPLE FILE — copy this to api_constants.dart and fill in real values.
/// api_constants.dart is gitignored so your keys are never committed.
class ApiConstants {
  ApiConstants._();

  /// OpenAI API key — get from https://platform.openai.com/api-keys
  static const String openAiKey = '';
}
